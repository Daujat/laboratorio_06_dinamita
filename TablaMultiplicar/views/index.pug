doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Tabla de Multiplicar
    style
      body {
          font-family: Arial, sans-serif;
          margin: 0;
          padding: 20px;
      }
      table {
          border-collapse: collapse;
          width: 100%;
          max-width: 400px;
      }
      th, td {
          border: 1px solid #ddd;
          padding: 8px;
          text-align: center;
      }
      th {
          background-color: #f2f2f2;
      }
  body
    h1 Tabla de Multiplicar
    table
      thead
        tr
          th Número
          th Tabla de Multiplicar
      tbody
        - for (let i = 1; i <= 10; i++)
          tr
            td= numero
            td= numero + ' x ' + i + ' = ' + (numero * i)